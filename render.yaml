services:
  - type: web
    name: lovable-library-frontend
    env: static
    buildCommand: npm install && npm run build && cp public/_redirects dist/_redirects
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
